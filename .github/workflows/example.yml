name: Example Usage

on:
  workflow_dispatch:
    inputs:
      image:
        description: 'Image to scan'
        required: true
        default: 'nginx:latest'

permissions:
  contents: write
  packages: read

jobs:
  submit:
    uses: ./.github/workflows/submit-dependencies.yml
    with:
      image: ${{ inputs.image }}
